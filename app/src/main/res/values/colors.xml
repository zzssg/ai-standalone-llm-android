<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Only the launch window background lives here; every colour the user
        actually sees comes from the Compose palette in ui/theme/Theme.kt.
        Matching this to the Compose light background stops the white flash
        between the launch window and the first frame.
    -->
    <color name="launch_background">#FAFDFB</color>
</resources>
